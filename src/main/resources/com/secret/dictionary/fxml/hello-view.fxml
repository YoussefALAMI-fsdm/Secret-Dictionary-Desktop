<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane fx:controller="com.secret.dictionary.controller.ControllerFX"
            xmlns:fx="http://javafx.com/fxml"
            styleClass="root-pane"
            style="-fx-padding: 20;">

    <!-- ZONE SUPÉRIEURE : Bouton Recherche -->
    <top>
        <HBox spacing="5" styleClass="top-bar">
            <HBox spacing="5" styleClass="search-bar">
                <Button fx:id="btnRecherche" text="🔍 Recherche"
                        styleClass="btn-primary"
                        onAction="#onRechercheClick"/>
            </HBox>
        </HBox>
    </top>

    <!-- ZONE GAUCHE : Menu latéral -->
    <left>
        <VBox fx:id="vboxLeft" spacing="5" styleClass="left-sidebar">
            <!-- Bouton Catégories avec sous-menu -->
            <VBox spacing="5">
                <Button fx:id="btnCatego" text="📁 Catégories"
                        styleClass="btn-primary"
                        onAction="#toggleMenu"/>

                <!-- Sous-menu caché (initialement) -->
                <VBox fx:id="menuCatego" spacing="5"
                      visible="false"
                      managed="false"
                      styleClass="submenu">
                    <Button text="• Tous les mots"
                            styleClass="btn-submenu"
                            onAction="#onTousLesMotsClick"/>
                    <Button text="• Verbes" styleClass="btn-submenu"/>
                    <Button text="• Adjectifs" styleClass="btn-submenu"/>
                    <Button text="• Noms" styleClass="btn-submenu"/>
                    <Button text="• Adverbes" styleClass="btn-submenu"/>
                    <Button text="• Expressions" styleClass="btn-submenu"/>
                </VBox>
            </VBox>

            <!-- Groupe de boutons (Favoris, Révision, Étiquettes) -->
            <VBox fx:id="btnFavorisGroup" spacing="5">
                <Button fx:id="btnFavoris" text="📌 Favoris" styleClass="btn-primary"/>
                <Button fx:id="btnRevision" text="📝 Révision" styleClass="btn-primary"/>
                <Button fx:id="btnEtiquettes" text="🏷️ Étiquettes" styleClass="btn-primary"/>
            </VBox>

            <!-- Spacer pour pousser le bouton Nouveau en bas -->
            <Region VBox.vgrow="ALWAYS" style="-fx-min-height: 30px;"/>

            <!-- Bouton Nouveau en bas -->
            <VBox fx:id="btnNouveauGroup" spacing="5">
                <Button fx:id="btnNouveau" text="➕ Nouveau"
                        styleClass="btn-primary"
                        onAction="#onNouveauClick"/>
            </VBox>
        </VBox>
    </left>

    <!-- ZONE CENTRALE : Détails du mot sélectionné -->
    <center>
        <ScrollPane fitToWidth="true" styleClass="center-scroll">
            <VBox fx:id="vboxCenter" spacing="15" styleClass="center-content">

                <!-- Titre du mot -->
                <Label fx:id="wordTitle" text="Sélectionnez un mot"
                       styleClass="word-title"/>

                <!-- Section Définition -->
                <VBox spacing="5" styleClass="definition-section">
                    <Label text="📖 Définition" styleClass="section-label"/>
                    <Label fx:id="definitionText" text="..."
                           styleClass="section-text"
                           wrapText="true"/>
                </VBox>

                <!-- Section Synonymes -->
                <VBox spacing="5" styleClass="synonyms-section">
                    <Label text="🔗 Synonymes" styleClass="section-label"/>
                    <Label fx:id="synonymsText" text="..." styleClass="section-text"/>
                </VBox>

            </VBox>
        </ScrollPane>
    </center>

    <!-- ZONE DROITE : Liste des mots -->
    <right>
        <VBox fx:id="vboxRight" spacing="20" styleClass="right-sidebar">
            <Label fx:id="rightTitle" text="Tous les mots" styleClass="right-title"/>

            <ListView fx:id="wordList" styleClass="word-listview"/>
        </VBox>
    </right>
</BorderPane>