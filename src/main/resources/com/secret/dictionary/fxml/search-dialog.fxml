<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox fx:controller="com.secret.dictionary.controller.SearchDialogController"
      xmlns:fx="http://javafx.com/fxml"
      spacing="15"
      styleClass="dialog-content">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Titre -->
    <Label text="Entrez le mot à rechercher"
           styleClass="dialog-title"/>

    <!-- Zone autocomplétion -->
    <VBox fx:id="autocompleteBox"
          spacing="0"
          maxWidth="400"
          prefWidth="400">

        <!-- Champ de saisie -->
        <TextField fx:id="txtRecherche"
                   promptText="Tapez pour rechercher..."
                   styleClass="dialog-textfield"
                   prefWidth="400"/>

        <!-- Liste de suggestions -->
        <ListView fx:id="suggestionList"
                  visible="false"
                  managed="false"
                  maxHeight="180"
                  styleClass="suggestion-list"/>
    </VBox>

    <!-- Info et compteur -->
    <Label text="💡 La recherche floue trouve les mots similaires"
           styleClass="dialog-info"/>

    <Label fx:id="resultCount"
           text=""
           visible="false"
           styleClass="result-count"/>

</VBox>