<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox fx:controller="com.secret.dictionary.controller.MenuController"
      xmlns:fx="http://javafx.com/fxml"
      styleClass="left-panel"
      spacing="5">

    <!-- Section Catégories avec sous-menu -->
    <VBox spacing="5">
        <Button fx:id="btnCatego"
                text="📁 Catégories"
                styleClass="btn-primary"
                onAction="#toggleMenu"/>

        <VBox fx:id="menuCatego"
              spacing="5"
              visible="false"
              managed="false"
              styleClass="submenu"
              style="-fx-padding: 0 0 0 20;">
            <Button text="• Tous les mots"
                    styleClass="btn-submenu"
                    onAction="#onTousLesMotsClick"/>
            <Button text="• Verbes"
                    styleClass="btn-submenu"
                    onAction="#onVerbsClick"/>
            <Button text="• Adjectifs"
                    styleClass="btn-submenu"
                    onAction="#onAdjectifsClick"/>
            <Button text="• Noms"
                    styleClass="btn-submenu"
                    onAction="#onNomsClick"/>
            <Button text="• Adverbes"
                    styleClass="btn-submenu"
                    onAction="#onAdverbesClick"/>
            <Button text="• Expressions"
                    styleClass="btn-submenu"
                    onAction="#onExpressionsClick"/>
        </VBox>
    </VBox>

    <!-- Section Favoris/Révision -->
    <VBox fx:id="btnFavorisGroup" spacing="5">
        <Button text="📌 Favoris"
                styleClass="btn-primary"
                onAction="#onFavorisClick"/>
        <Button text="📝 Révision"
                styleClass="btn-primary"
                onAction="#onRevisionClick"/>
        <Button text="🏷 Étiquettes"
                styleClass="btn-primary"
                onAction="#onEtiquettesClick"/>

        <!-- Espaceur -->
        <Region VBox.vgrow="ALWAYS" style="-fx-min-height: 30px;"/>

        <!-- Bouton Ajouter -->
        <VBox fx:id="btnNouveauGroup" spacing="5">
            <Button fx:id="btnNouveau"
                    text="➕ Ajouter"
                    styleClass="btn-primary btn-add"
                    onAction="#onNouveauClick"/>
        </VBox>
    </VBox>
</VBox>