<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane fx:id="rootPane"
            fx:controller="com.secret.dictionary.controller.MainController"
            xmlns:fx="http://javafx.com/fxml"
            styleClass="root-pane"
            stylesheets="@../styles/main.css"
            style="-fx-padding: 5 20 5 20;">

    <!-- Barre supérieure -->
    <top>
        <HBox spacing="5" styleClass="top-bar">
            <Button fx:id="btnRecherche"
                    text="🔍 Recherche"
                    styleClass="btn-primary"
                    onAction="#onRechercheClick"/>
        </HBox>
    </top>

    <!-- Panneau gauche : Menu -->
    <left>
        <VBox fx:id="leftPanel" styleClass="panel-container">
            <!-- Contenu chargé dynamiquement depuis menu-panel.fxml -->
        </VBox>
    </left>

    <!-- Zone centrale : Détails du mot -->
    <center>
        <VBox fx:id="centerPanel" styleClass="panel-container">
            <!-- Contenu chargé dynamiquement depuis word-details.fxml -->
        </VBox>
    </center>

    <!-- Panneau droit : Liste des mots -->
    <right>
        <VBox fx:id="rightPanel" styleClass="panel-container">
            <!-- Contenu chargé dynamiquement depuis word-list.fxml -->
        </VBox>
    </right>
</BorderPane>