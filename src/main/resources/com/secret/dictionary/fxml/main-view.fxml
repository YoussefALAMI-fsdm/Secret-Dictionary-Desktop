<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<BorderPane fx:id="rootPane"
            fx:controller="com.secret.dictionary.controller.MainController"
            xmlns:fx="http://javafx.com/fxml"
            styleClass="root-pane"
            style="-fx-padding: 5 20 5 20;">

    <!-- BARRE SUPÉRIEURE -->
    <top>
        <HBox fx:id="topBar" spacing="5" style="-fx-background: transparent; -fx-padding: 0 0 15 0;">
            <HBox spacing="5" style="-fx-padding: 10; -fx-background: transparent;">
                <Button text="🔍 Recherche" styleClass="btn-primary"
                        style="-fx-font-size: 14px; -fx-background-color: #007acc;"
                        onAction="#onRechercheClick"/>
            </HBox>
        </HBox>
    </top>

    <!-- PANNEAU GAUCHE (Menu latéral) -->
    <left>
        <HBox fx:id="leftContainer" style="-fx-padding: 0 15 0 0;">
            <!-- Le contenu sera chargé dynamiquement depuis side-menu.fxml -->
        </HBox>
    </left>

    <!-- PANNEAU CENTRAL (Détails du mot) -->
    <center>
        <HBox fx:id="centerContainer" style="-fx-padding: 0 15 0 15;">
            <!-- Le contenu sera chargé dynamiquement depuis word-details.fxml -->
        </HBox>
    </center>

    <!-- PANNEAU DROIT (Liste des mots) -->
    <right>
        <HBox fx:id="rightContainer" style="-fx-padding: 0 0 0 15;">
            <!-- Le contenu sera chargé dynamiquement depuis word-list.fxml -->
        </HBox>
    </right>
</BorderPane>