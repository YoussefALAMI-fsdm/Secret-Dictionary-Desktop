<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<ScrollPane fitToWidth="true"
            styleClass="stats-scroll"
            style="-fx-background: transparent;"
            HBox.hgrow="ALWAYS"
            fx:controller="com.secret.dictionary.controller.StatisticsViewController"
            xmlns:fx="http://javafx.com/fxml">

    <VBox fx:id="statsContainer"
          spacing="25"
          alignment="TOP_CENTER"
          style="-fx-padding: 30 40;">

        <!-- EN-TÊTE -->
        <VBox spacing="10" alignment="CENTER">
            <Label text="📊"
                   style="-fx-font-size: 56px;"/>

            <Label fx:id="titleLabel"
                   text="Statistiques du Dictionnaire"
                   styleClass="stats-title"/>

            <Label fx:id="totalCountLabel"
                   text="0 mots au total"
                   styleClass="stats-subtitle"/>
        </VBox>

        <!-- SÉPARATEUR -->
        <Region styleClass="stats-separator"
                prefHeight="2"
                maxWidth="200"/>

        <!-- LISTE DES CATÉGORIES -->
        <VBox fx:id="categoriesBox"
              spacing="15"
              alignment="TOP_CENTER"
              style="-fx-min-width: 100px; -fx-max-height:50px; -fx-max-width: 800px;">
            <!-- Les cartes de catégories seront ajoutées dynamiquement -->
        </VBox>

        <!-- FOOTER INFO -->
        <VBox spacing="8" alignment="CENTER" style="-fx-padding: 20 0 0 0;">
            <Label text="💡 Cliquez sur un mot dans la liste pour voir ses détails"
                   styleClass="stats-hint"/>
            <Label text="🔍 Utilisez la recherche pour trouver un mot spécifique"
                   styleClass="stats-hint"/>
        </VBox>

    </VBox>
</ScrollPane>