<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox fx:id="vboxLeft"
      fx:controller="com.secret.dictionary.controller.MenuController"
      xmlns:fx="http://javafx.com/fxml"
      styleClass="left-panel"
      spacing="5">

    <!-- SECTION CATÉGORIES -->
    <VBox spacing="5">
        <Button fx:id="btnCatego" text="📁 Recherche avancée"
                onAction="#toggleMenu"
                style="-fx-font-size: 14px; -fx-background-color: #007acc;">
            <tooltip>
                <Tooltip text="Recherche avancée" showDelay="200ms"/>
            </tooltip>
        </Button>

        <VBox fx:id="menuCatego" spacing="5" visible="false" managed="false"
              style="-fx-padding: 0 0 0 20;">
            <Button text="• Emojis"
                    onAction="#onTousLesMotsClick"
                    style="-fx-font-size: 12px; -fx-background-color: #79c0ff"/>

            <Button text="• Recherche synonymes"
                    style="-fx-font-size: 12px; -fx-background-color: #79c0ff">
                <tooltip>
                    <Tooltip text="Recherche synonymes" showDelay="200ms"/>
                </tooltip>
            </Button>

            <Button text="• Recherche antonymes"
                    style="-fx-font-size: 12px; -fx-background-color: #79c0ff">
                <tooltip>
                    <Tooltip text="Recherche antonymes" showDelay="200ms"/>
                </tooltip>
            </Button>
        </VBox>
    </VBox>

    <!-- SECTION FAVORIS ET RÉVISION -->
    <VBox fx:id="btnFavorisGroup" spacing="5">
        <!-- ✅ BOUTON SYNONYME CONNECTÉ -->
        <Button text=" 🔗 Ajout de synonyme"
                onAction="#onAjoutSynonymeClick"
                style="-fx-font-size: 14px; -fx-background-color: #007acc;">
            <tooltip>
                <Tooltip text="Ajout de synonyme" showDelay="200ms"/>
            </tooltip>
        </Button>

        <!-- ✅ BOUTON ANTONYME CONNECTÉ -->
        <Button text=" ⚡ Ajout de antonyme"
                onAction="#onAjoutAntonymeClick"
                style="-fx-font-size: 14px; -fx-background-color: #007acc;">
            <tooltip>
                <Tooltip text="Ajout de antonyme" showDelay="200ms"/>
            </tooltip>
        </Button>

        <Region VBox.vgrow="ALWAYS" style="-fx-min-height: 30px;"/>

        <!-- BOUTON AJOUTER -->
        <VBox fx:id="btnNouveauGroup" spacing="5">
            <Button text="➕ Ajouter"
                    style="-fx-font-size: 12px; -fx-background-color: #007acc;"
                    styleClass="btn-primary"
                    onAction="#onNouveauClick"/>
        </VBox>
    </VBox>
</VBox>