<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<VBox fx:controller="com.secret.dictionary.controller.AddWordDialogController"
      xmlns:fx="http://javafx.com/fxml"
      spacing="15"
      styleClass="dialog-content">

    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>

    <!-- Titre -->
    <Label text="Créer une nouvelle entrée"
           styleClass="dialog-title"/>

    <!-- Champ Mot -->
    <VBox spacing="5">
        <Label text="📝 Mot *"
               styleClass="dialog-field-label"/>
        <TextField fx:id="txtMot"
                   promptText="Exemple : Époustouflant"
                   styleClass="dialog-textfield"
                   prefWidth="400"/>
    </VBox>

    <!-- Champ Définition -->
    <VBox spacing="5">
        <Label text="📖 Définition"
               styleClass="dialog-field-label"/>
        <TextArea fx:id="txtDef"
                  promptText="Entrez la définition du mot..."
                  styleClass="dialog-textarea"
                  prefRowCount="4"
                  prefWidth="400"
                  wrapText="true"/>
    </VBox>

    <!-- Information -->
    <Label text="* Champ obligatoire"
           styleClass="dialog-info-required"/>

</VBox>