<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<ScrollPane fx:controller="com.secret.dictionary.controller.WordDetailsController"
            xmlns:fx="http://javafx.com/fxml"
            fitToWidth="true"
            styleClass="center-scroll">

    <VBox spacing="15" styleClass="center-content">

        <!-- Titre du mot -->
        <Label fx:id="wordTitle"
               text="Sélectionnez un mot"
               styleClass="word-title"/>

        <!-- Section Définition -->
        <VBox spacing="5" styleClass="definition-section">
            <Label text="📖 Définition"
                   styleClass="section-title"/>
            <Label fx:id="definitionText"
                   text="..."
                   styleClass="section-content"
                   wrapText="true"/>
        </VBox>

        <!-- Section Synonymes -->
        <VBox spacing="5" styleClass="synonyms-section">
            <Label text="🔗 Synonymes"
                   styleClass="section-title"/>
            <Label fx:id="synonymsText"
                   text="..."
                   styleClass="section-content"/>
        </VBox>

        <!-- Espace pour futures sections -->
        <Region VBox.vgrow="ALWAYS"/>

    </VBox>
</ScrollPane>