<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<ScrollPane fitToWidth="true"
            styleClass="center-scroll"
            style="-fx-background: transparent;"
            HBox.hgrow="ALWAYS"
            fx:controller="com.secret.dictionary.controller.WordDetailsController"
            xmlns:fx="http://javafx.com/fxml">

    <VBox fx:id="vboxCenter"
          spacing="15"
          style="-fx-padding: 20; -fx-background-color: transparent;">

        <!-- ✅ BOUTON MODIFIER EN HAUT À DROITE (SÉPARÉ) -->
        <HBox alignment="TOP_RIGHT">
            <Button fx:id="btnModifier"
                    text="✏️ Modifier"
                    styleClass="btn-primary"
                    onAction="#onModifierClick"
                    visible="false"
                    managed="false"
                    style="-fx-font-size: 14px; -fx-background-color: #007acc; -fx-cursor: hand;"/>
        </HBox>

        <!-- EN-TÊTE AVEC MOT + ÉMOJI -->
        <HBox spacing="15" alignment="CENTER_LEFT">
            <!-- ÉMOJI (caché par défaut) -->
            <Label fx:id="emojieLabel"
                   text="📚"
                   visible="false"
                   managed="false"
                   styleClass="word-emoji"
                   style="-fx-font-size: 48px;"/>

            <!-- TITRE DU MOT + CATÉGORIE -->
            <VBox spacing="5">
                <Label fx:id="wordTitle"
                       text="Sélectionnez un mot"
                       styleClass="word-title"
                       style="-fx-font-size: 32px; -fx-text-fill: #27C5F5; -fx-font-weight: bold;"/>

                <!-- CATÉGORIE (cachée par défaut) -->
                <Label fx:id="categorieLabel"
                       text="🏷️ General"
                       visible="false"
                       managed="false"
                       styleClass="word-category"
                       style="-fx-font-size: 14px; -fx-text-fill: #b185db; -fx-font-style: italic;"/>
            </VBox>
        </HBox>

        <!-- SECTION DÉFINITION -->
        <VBox spacing="5"
              style="-fx-background-color: #0B0B59; -fx-padding: 15; -fx-background-radius: 15;">
            <Label text="📖 Définition"
                   style="-fx-font-size: 16px; -fx-text-fill: #4CAF50; -fx-font-weight: bold;"/>
            <Label fx:id="definitionText"
                   text="..."
                   style="-fx-text-fill: #ccc; -fx-font-size: 14px;"
                   wrapText="true"/>
        </VBox>

        <!-- SECTION SYNONYMES -->
        <VBox maxWidth="200"
              prefHeight="90"
              spacing="5"
              style="-fx-background-color: #0B0B59; -fx-padding: 15; -fx-background-radius: 15;">
            <Label text="🔗 Synonymes"
                   style="-fx-font-size: 16px; -fx-text-fill: #2196F3; -fx-font-weight: bold;"/>
            <Label fx:id="synonymsText"
                   text="..."
                   style="-fx-text-fill: #ccc; -fx-font-size: 13px;"/>
        </VBox>

    </VBox>
</ScrollPane>